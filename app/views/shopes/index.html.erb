<table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Company</th>
      <th scope="col">Action</th>
    </tr>
  </thead>
  <tbody>
    <% @shopes.each do |mob|%>
      <tr>
        <th scope="row">1</th>
        <td><%=  mob.product_name%></td>
        <td><%=  link_to "Edit",  edit_shope_path(mob.id)%></td>
        <td><%=  link_to "Show", shope_path(mob.id)%></td>
        <td><%=  button_to  'Delete', shope_path(mob), method: 'delete' %></td>
    <% end%>
  </tbody>
</table>
